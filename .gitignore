# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Dependencies
node_modules/
# package-lock.json is intentionally NOT ignored: `npm ci` in the deploy
# workflow requires it, and it pins the build to known-good versions.

# Build output (produced by the deploy workflow, never committed)
dist/
build/

# Environment variables
.env
.env.local

# Local tooling and its state (not part of the site)
.claude/
.impeccable/

# Editor workspace file (local to this machine)
portfolio-website.code-workspace

# Original source uploads (clean-named copies are what the site uses).
# "RESUME - Tasneem Iqbal (DoorDash).pdf" is byte-identical to
# Tasneem_Iqbal_Resume.pdf, which is the copy the site actually links.
TasneemIqbal_Resume_June copy.pdf
Product Spec for Schedaddle.pdf
RESUME - Tasneem Iqbal (DoorDash).pdf
public/v1/images/Marketing Landing.png
public/v1/images/Schedaddle image.png

# Walkthrough recording of the pre-redesign DIG site, 99MB. The frames the case
# study uses are extracted from it as src/assets/dig-old-*.jpg.
src/assets/*.mov
